name: Codo
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - anthropic>=0.40.0
      - pydantic>=2.0.0
      - aiofiles>=23.0.0
      - python-dotenv>=1.0.0
      - pywebview>=5.0
