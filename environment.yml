name: Codo
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - anthropic>=0.40.0
      - click>=8.1.0
      - rich>=13.0.0
      - textual>=0.50.0
      - pydantic>=2.0.0
      - aiofiles>=23.0.0
      - python-dotenv>=1.0.0
